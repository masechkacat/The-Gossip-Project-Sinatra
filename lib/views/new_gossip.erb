<!DOCTYPE html>
<html>
  <head>
    <title>Créer un potin</title>
    <style>
      body {
        flex-direction: column;
        display: flex;
        align-items: center;
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: paleturquoise;
      }
      header {
          text-align: center;
          padding: 4%;
          color: blue;
        }
      
      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        /* text-align: center; */
        padding: 4%;
        background-color: honeydew;
        width: 50%;
        border-radius: 10px;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
      }

      h1 {
        font-size: 2em;
      }

      p {
        padding-bottom: 10px;
      }

      label {
        display: block;
        font-weight: bold;
        margin: 10px 0;
      }

      input[type="text"],
      textarea {
        width: 85%;
        padding: 10px;
        margin: 5px 0;
        border: 1px solid #ddd;
        border-radius: 10px;
      }

      input[type="submit"] {
        background-color: blue;
        color: #fff;
        padding: 10px 15px;
        text-align: center;
        border: none;
        border-radius: 10px;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);
      }

      input[type="submit"]:hover {
        background-color: #0053b3;
      }

      .add-link {
        color: #fff;
        text-decoration: none;
        background-color: blue;
        padding: 10px 15px;
        text-align: center;
        margin: 10px auto;
        border-radius: 10px;
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.2);      
      }

      .add-link:hover {
        background-color: #0053b3;
      }

    </style>
  </head>
  <body>
  <header>
    <h1>Crée un potin !</h1>
    <p>Balance ton potin via le formulaire ci-dessous</p>
    <a href="/" class = "add-link">Page d'accueil</a>
  </header>
    
    <form action="/gossips/new" method="post">
      <label for="author">Saisi ici ton nom :</label>
      <input type="text" name="author" id="author" required>
      <br>
      <label for="content">Balance ton potin :</label>
      <textarea name="content" id="content" rows="4" required></textarea>
      <br>
      <input type="submit" value="Créer le potin">
    </form>
  </body>
</html>
